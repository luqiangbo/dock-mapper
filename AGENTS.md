# DockMapper 项目规则

## 适用范围

本文件适用于整个仓库。子目录存在更具体的 `AGENTS.md` 时，以更近的规则为准。

## 项目概况

- 目标平台为 Windows 11 x64，使用 PowerShell，不使用 WSL。
- 桌面端由 Tauri 2、Rust、React 19 和 TypeScript 构成，包管理器固定使用 pnpm。
- 截图、OCR、贴图和配置数据默认只在本机处理；不要擅自增加云端依赖或遥测。
- 设置继续使用原子 JSON 事务；截图历史继续使用 PNG、manifest 和缩略图。
- 图片等大二进制 IPC 保留手写封装，不要改为 JSON/Specta 传输。

## 实现约束

- 先沿真实调用链定位问题：界面入口 → 前端状态 → API 薄封装 → Tauri 命令 → Rust 状态/持久化 → 再次读取。
- 截图浮层保持组合入口职责；新增交互优先放入对应 hook、面板或领域模块，避免继续扩大 `ScreenshotOverlay.tsx` 和 `lib.rs`。
- 对异步界面显式处理加载、空数据、成功和失败状态。不得使用无反馈的 `.catch(() => undefined)` 隐藏用户可感知错误。
- 弹层、二级菜单或选择器的可见性不能依赖“已有数据”；首次使用时也必须能打开并看到明确空状态。
- 持久化功能至少考虑首次为空、首次写入、重复写入/上限、重新打开后读取，以及写入失败。
- 捕获浮层中的异步结果必须受捕获代次约束，防止旧截图结果写入新会话。
- Windows 坐标、选区和窗口候选继续以物理像素及捕获代次校验，不能向前端暴露原始 HWND。
- OCR 模型相关测试必须标记 `#[ignore]`；不得在常规验证中初始化模型。
- 修改类型化 IPC 时同步更新绑定或薄封装；原始二进制 IPC 不纳入 JSON 绑定。

## 测试与交付证据

- 对新增或修复行为编写贴近状态转换的测试；测试名称应描述用户可观察结果，而不是内部实现。
- 类型检查通过不代表界面已可用，Rust 测试通过也不代表前端交互已验证。交付说明必须区分：静态检查、自动化行为测试、真实交互验证。
- 若当前环境无法操作 Tauri 窗口，明确列出未验证项和最短人工路径，不得声称功能已经端到端通过。
- 不运行 `pnpm build`、模型初始化测试、`pnpm format:check` 或 `cargo fmt --check`。
- 交付前只运行以下两项：
  1. 仓库根目录执行 `pnpm typecheck`。
  2. `src-tauri` 目录执行 `cargo test -- --skip model`。

## 完成定义

一次用户可见功能交付必须同时说明：改动入口、关键状态、数据是否持久化、错误如何呈现、执行过的验证，以及仍需人工确认的场景。没有对应证据时使用“已实现但未做真实交互验证”，不要写“已完全解决”。

