{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pinned-image",
  "description": "贴图窗口仅允许关闭自身和读取其内存图片",
  "windows": ["pin"],
  "permissions": [
    "core:default",
    "core:window:allow-close"
  ]
}
